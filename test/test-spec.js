describe('Hello world', () => {

  beforeEach(() => {
  });

  afterEach(() => {
  });

  it('adds numbers', function () {
    console.assert( (2+3) === 5);
  });

  it('concatenates strings', function () {
    console.assert( ('foo' + 'bar') === 'foobar');
  });
});
